name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: ls
      run: ls api
    - name: Build the Docker api image
      run: cd ./api && docker build . --tag my-image-name:$(date +%s)
    - name: Build the Docker auth image
      run: cd ./auth && docker build . --tag my-image-name:$(date +%s)
